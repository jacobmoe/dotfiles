# Dependencies

```sh
$ brew install git
$ brew install git-completion
$ brew install node
$ brew install ag
$ brew install ispell
$ brew install chruby
$ brew install ruby-install
$ ruby-install
```
